post:
  tags:
    - Actor tasks
  summary: Reboot last run
  description: |
    Reboots the last actor task run. Redispatches to [Reboot run](/api/v2/actor-run-reboot-post).

    Using the `status` query parameter you can ensure to only get a run with a certain status
    (e.g. `status=SUCCEEDED`). Only the most recent run with that status is used.
  operationId: actorTask_runs_last_reboot_post
  parameters:
    - $ref: ../../components/parameters/actorTaskId.yaml
    - $ref: ../../components/parameters/runs-last/status.yaml
  responses:
    "200":
      $ref: ../../components/responses/actor-runs/reboot.yaml
    "400":
      $ref: ../../components/responses/BadRequest.yaml
    "404":
      description: Not found - the requested resource was not found.
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/common/ErrorResponse.yaml
          examples:
            actor-not-found:
              $ref: ../../components/examples/actor-not-found.yaml
            actor-run-not-found:
              $ref: ../../components/examples/actor-run-not-found.yaml
  deprecated: false
