get:
  tags:
    - Actor tasks
  summary: Get queue head for last run
  description: |
    Returns the first requests from the default request queue of the last actor task run. Redispatches to
    [Get head](/api/v2/request-queue-head-get).

    Using the `status` query parameter you can ensure to only get a run with a certain status
    (e.g. `status=SUCCEEDED`). Only the most recent run with that status is used.
  operationId: actorTask_runs_last_requestQueue_head_get
  parameters:
    - $ref: ../../components/parameters/actorTaskId.yaml
    - $ref: ../../components/parameters/runs-last/status.yaml
    - name: limit
      in: query
      description: Maximum number of requests to return.
      style: form
      explode: true
      schema:
        type: number
        example: 100
    - name: clientKey
      in: query
      description: A unique identifier of the client accessing the request queue.
      style: form
      explode: true
      schema:
        type: string
  responses:
    "200":
      description: ""
      headers: {}
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/request-queues/HeadResponse.yaml
    "400":
      $ref: ../../components/responses/BadRequest.yaml
  deprecated: false
