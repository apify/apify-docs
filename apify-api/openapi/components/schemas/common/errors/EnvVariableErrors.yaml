EnvironmentVariableNotFoundError:
  type: object
  properties:
    error:
      type: object
      required: [type]
      properties:
        type:
          type: string
          enum: [record-not-found]
        message:
          type: string
          example: Environment variable was not found
