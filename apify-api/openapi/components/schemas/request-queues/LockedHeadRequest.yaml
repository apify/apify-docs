title: LockedHeadRequest
description: A request from the request queue head that has been locked for processing.
type: object
required:
  - id
  - uniqueKey
  - url
  - lockExpiresAt
properties:
  id:
    $ref: ./SharedProperties.yaml#/RequestId
  uniqueKey:
    $ref: ./SharedProperties.yaml#/UniqueKey
  url:
    type: string
    description: The URL of the request. May be truncated if it exceeds 128 characters.
    examples: [https://apify.com]
  method:
    $ref: ./SharedProperties.yaml#/RequestMethod
  retryCount:
    $ref: ./SharedProperties.yaml#/RetryCount
  lockExpiresAt:
    $ref: ./SharedProperties.yaml#/LockExpiresAt
