title: ListOfDatasets
allOf:
  - $ref: ../common/PaginationResponse.yaml
  - type: object
    required:
      - items
    properties:
      unnamed:
        type: boolean
        description: Whether the listing was filtered to only unnamed datasets.
        examples: [false]
      items:
        type: array
        items:
          $ref: ./DatasetListItem.yaml
