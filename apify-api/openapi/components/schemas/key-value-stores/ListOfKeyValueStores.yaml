title: ListOfKeyValueStores
allOf:
  - $ref: ../common/PaginationResponse.yaml
  - type: object
    required:
      - items
    properties:
      unnamed:
        type: boolean
        description: Whether the listing was filtered to only unnamed key-value stores.
        examples: [false]
      items:
        type: array
        items:
          $ref: ./KeyValueStore.yaml
