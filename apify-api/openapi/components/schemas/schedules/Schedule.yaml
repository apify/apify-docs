ScheduleShort:
  title: ScheduleShort
  allOf:
    - $ref: ./ScheduleBase.yaml
    - type: object
      required:
        - actions
      properties:
        actions:
          type: array
          items:
            $ref: ./ScheduleActionShort.yaml
          description: ""

Schedule:
  title: Schedule
  allOf:
    - $ref: ./ScheduleBase.yaml
    - type: object
      required:
        - actions
      properties:
        description:
          type: [string, "null"]
          examples: [Schedule of actor ...]
        title:
          type: [string, "null"]
        actions:
          type: array
          items:
            $ref: ./ScheduleAction.yaml
          description: ""
