title: GetBuildListResponse
type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - total
      - offset
      - limit
      - desc
      - count
      - items
    properties:
      total:
        type: number
        example: 2
      offset:
        type: number
        example: 0
      limit:
        type: number
        example: 1000
      desc:
        type: boolean
        example: false
      count:
        type: number
        example: 2
      items:
        type: array
        items:
          $ref: ./BuildShort.yaml
        description: ''
